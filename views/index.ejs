<!DOCTYPE html>
<html lang="en">
  <%- include('partials/header') %>
<body>
  <%- include('partials/navbar') %>

  <div class="container">
    <h1>Perguntas</h1>
    <br>
    <a href="/perguntar" class="btn btn-dark">Fazer pergunta</a>
    <hr>
    <% perguntas.forEach(perguntas => { %>
      <br>
      <div class="card">
        <div class="card-body">
          <h3><%= perguntas.titulo %> </h3>
        </div>
        <div class="card-footer">
          <a href="/pergunta/<%= perguntas.id %>"</a><button class="btn btn-dark">Responder</button></a>
        </div>
      </div>
      
    <% }) %> 

  </div>
  <%- include('partials/footer') %> 
</body>
</html>